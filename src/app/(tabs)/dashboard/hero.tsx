import React from "react";
import { ScrollView, View } from "react-native";

type Props = {};

const HeroDashboard = (props: Props) => {
  return (
    <ScrollView>
      <View style={{ margin: "auto" }}>HeroDashboard</View>
    </ScrollView>
  );
};
