import React from "react";
import { ScrollView, View } from "react-native";

type Props = {};

const CompoundingReturnCalculator = (props: Props) => {
  return (
    <ScrollView>
      <View style={{ margin: "auto" }}>CompoundingReturnCalculator</View>
    </ScrollView>
  );
};
