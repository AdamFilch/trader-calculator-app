import React from "react";
import { ScrollView, View } from "react-native";

type Props = {};

const ProfitLossReturnCalculator = (props: Props) => {
  return (
    <ScrollView>
      <View style={{ margin: "auto" }}>ProfitLossReturnCalculator</View>
    </ScrollView>
  );
};
